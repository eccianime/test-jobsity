import { InformationTabProps } from "@/types/components";
import React from "react";
import { View } from "react-native";

export default function EpisodesTab({ data }: InformationTabProps) {
  return <View className="rounded-b-2xl bg-white p-4"></View>;
}
